# WF-07 学期任务：数据库架构规划记录

> 分支：`wf03-wf12-architecture-planning`
>
> 工作方式：一次只讨论并确认一个节点。当前阶段冻结可实现的表结构、输入输出、代码、分支和连线，但暂不要求在讯飞星辰平台创建数据表或完成变量绑定。
>
> 资料基线：以用户本次提供的 WF-07 系统提示词和大模型节点截图为唯一业务底稿；数据库化只迁移状态读写方式，不擅自删改原业务规则。

## 阅读与执行方式

本文供后续负责讯飞星辰平台配置的同学直接执行。每个节点确认后，必须记录：节点名称、前后连线、节点设置、数据范围或输入、字段或输出、业务作用、禁止事项与易错点、完成检查。

文档状态必须区分：

- **已确认设计**：已经与用户讨论通过，可以按文档实施。
- **草案待确认**：仅供讨论，不能直接建表或绑定变量。
- **平台已验证**：已经在讯飞星辰实际运行并通过测试。

除非明确标记“平台已验证”，不得把规划描述成已经在画布完成。

## 一、已确认的业务基线

WF-07 在当前会话中创建、查看、完成、延期、调整或取消学期任务。

固定规则：

1. 首次创建 3～5 个任务，覆盖最近四周，先形成 draft 并等待确认。
2. 查看已有 confirmed 时只在 reply 中摘要展示，不制造新草稿。
3. 完成任务必须明确任务并提供实际结果或证据；证据不足时追问，不能标记完成。
4. 延期必须明确任务、新日期或时间范围、延期原因；缺一项就追问。
5. 取消指定任务只改变任务状态，不删除任务记录。
6. 调整基于已有 confirmed 创建 draft，并保留未被点名的任务。
7. 所有正式任务变更都先进入 draft 和 `awaiting_confirmation`，确认后才复制到 confirmed。
8. `task_id` 在当前状态中唯一。
9. 不得声称已写入日历或发送提醒。

## 二、上游信息优先级与边界

| 优先级 | 数据来源 | 处理方式 |
|---:|---|---|
| 1 | WF-06 `plan_confirmed` | 根据已确认主规划生成任务 |
| 2 | WF-01 `profile_confirmed` | 没有主规划时生成临时任务清单，并明确提醒其临时性质 |
| 3 | 两者均为空 | 询问用户目标，或仅生成谨慎的临时清单；不得编造画像、规划或截止日期 |

WF-06 和 WF-01 都是可选增强信息，不是进入 WF-07 的硬门槛。不得修改任何上游 confirmed 数据，也不得因上游缺失强制退回其他模块。

## 三、已确认的状态机与动作语义

### 模块状态

统一使用：

- `not_started`
- `collecting`
- `awaiting_confirmation`
- `complete`
- `cancelled`
- `clarification_needed`

### 单个任务状态

严格使用 confirmed 业务结构中的中文枚举：

- `待处理`
- `完成`
- `推迟`
- `取消`

### 原提示词歧义的固定解释

1. “查看”已有 confirmed 后，模块 `current_status` 和 `task_status` 使用英文 `complete`，不是中文“完成”。
2. 原文“取消指定任务状态改为 cancelled”按 confirmed 业务结构解释为单个任务状态 `取消`；模块整体取消才使用 `cancelled`。
3. 原文“基于 confirm 创建 draft”解释为基于已有 `confirmed` 创建修订草稿。
4. “取消当前草稿”和“取消指定任务”必须区分：前者清空 draft；后者创建任务变更草稿，等待确认。
5. 没有可确认草稿时，confirm 不得完成模块。

## 四、draft 与 confirmed 业务结构基线

```json
{
  "学期":"",
  "任务":[
    {
      "task_id":"T01",
      "任务":"",
      "优先级":"高|中|低",
      "截止日期":"",
      "状态":"待处理|完成|推迟|取消",
      "success_criteria":[],
      "expected_evidence":[],
      "actual_evidence":[],
      "delay_reason":""
    }
  ],
  "weekly_focus":[],
  "摘要":""
}
```

固定约束：

- 首次生成 3～5 个任务；状态容量上限仍为 10 个任务。
- 普通数组最多 5 项，warnings 最多 3 项。
- task_id 必须非空且在同一对象中唯一。
- 完成任务时 `actual_evidence` 必须非空。
- 推迟任务时新截止日期或时间范围与 `delay_reason` 必须同时存在。
- 未被用户点名的任务必须保留。
- 不保存冗长分析、面向用户的完整回复或知识库原文。

## 五、当前大模型节点基线

| 配置项 | 当前值 |
|---|---|
| 当前输入 | `user_input`、`router_context` |
| 当前输出格式 | text |
| 当前唯一输出 | `output`，String |
| 当前模型 | Kimi-K2.5 |
| 对话历史 | 关闭 |

数据库化后预计增加：

- `profile_state_rows`
- `plan_state_rows`
- `task_state_rows`

输出仍保持单一 `output:String`。正式迁移时以用户提供的原提示词为底稿做最小修改，保留所有任务动作、证据门槛、草稿确认、取消语义、容量规则和确认回复，只把完整 prior state 改为数据库输入与增量输出。

## 六、WF-07 模块表字段基线

建议表名：`wf07_semester_task_records`

| 字段 | 类型 | 默认值 | 用途 |
|---|---|---|---|
| `task_status` | String | `not_started` | WF-07 当前模块状态 |
| `task_draft` | String | `{}` | 收集或待确认的任务对象 |
| `task_confirmed` | String | `{}` | 用户已确认的任务对象 |
| `task_version` | Integer | `1` | 模块记录版本 |
| `last_user_intent` | String | `未记录` | 用户本轮原话 |
| `warnings` | String | `[]` | 本模块警告数组的 JSON 字符串 |

六个字段是后续读取、初始化、统一重新读取、大模型输入、C01 输出和模块写回的固定合同。任务数组及其全部属性都保存在 draft 或 confirmed JSON 中，不为 task_id、证据、截止日期、延期原因等另加数据库列。

回复不写数据库，继续通过共享 `last_reply`、现有 N90 和唯一结束节点返回。

当前状态：业务基线、上游边界、状态机、业务结构和六字段模块表已经讨论确认并归档；讯飞星辰平台尚未实际建表。

## 七、预期主链路骨架

```text
M03 MAIN 总分支器的 WF-07 出口
→ 读取 WF-01 已确认画像
→ 读取 WF-06 已确认主规划
→ 读取 WF-07 任务状态
→ 判断记录是否存在
→ 必要时初始化
→ 统一重新读取 WF-07 当前状态
→ WF-07 大模型
→ C01 解析模型输出
→ 解析是否成功
→ 更新 WF-07 模块表
→ C02 合并公共路由状态
→ 更新公共路由表
→ 写入共享 last_reply
→ 现有 N90
→ 唯一结束节点
```

本节只冻结总体方向，不代表所有节点配置已经确认。后续仍按“一次一个节点、用户确认后立即归档推送”的方式推进。

## 八、逐节点确认记录

### 第 1 个节点：`WF07_DB01_读取画像状态`

#### 前后连线

```text
M03 MAIN 总分支器的 WF-07 出口
→ WF07_DB01_读取画像状态
```

本节点完成后先停止；下一节点单独讨论。

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf01_profile_records` |
| 处理模式 | 查询数据 |
| 查询上限 | `1` |
| 排序 | 留空 |
| 输出 | 保持默认的 `isSuccess`、`message`、`outputList` |

#### 设置数据范围

| 表字段 | 条件 | 值类型 | 比较值 |
|---|---|---|---|
| `profile_version` | 大于 | 输入 | 数字 `0` |

#### 查询结果字段

- `profile_status`
- `profile_confirmed`
- `profile_version`

不读取 `profile_draft`。

#### 业务作用

当 WF-06 没有已确认主规划时，WF-07 可以参考可解析的非空 `profile_confirmed` 生成临时任务清单。没有画像、画像为空或无法解析时仍须继续，不能强制返回 WF-01，也不能编造画像信息。

#### 禁止事项与易错点

1. `profile_version > 0` 只表示记录存在，不表示画像已确认。
2. 是否可用必须由后面的大模型检查非空 `profile_confirmed`。
3. 不得读取或修改 `profile_draft`。
4. 不得增加“无画像返回 WF-01”的分支。
5. 本节点只查询，不写入任何数据。
6. 不得误选 WF-07 自身模块表。
7. 默认输出不得修改。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 上游连接 MAIN 总分支器的 WF-07 出口。
- [ ] 数据库选择 `university_planner`。
- [ ] 数据表选择 `wf01_profile_records`。
- [ ] 处理模式为查询数据。
- [ ] 查询上限为 1，排序留空。
- [ ] 数据范围为 `profile_version > 0`。
- [ ] 只读取 status、confirmed 和 version。
- [ ] 没有读取 draft。
- [ ] 默认输出保持不变。
- [ ] 没有增加画像前置门槛。

### 第 2 个节点：`WF07_DB02_读取主规划状态`

#### 前后连线

```text
WF07_DB01_读取画像状态
→ WF07_DB02_读取主规划状态
```

本节点完成后先停止；下一节点单独讨论。

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf06_main_plan_records` |
| 处理模式 | 查询数据 |
| 查询上限 | `1` |
| 排序 | 留空 |
| 输出 | 保持默认的 `isSuccess`、`message`、`outputList` |

#### 设置数据范围

| 表字段 | 条件 | 值类型 | 比较值 |
|---|---|---|---|
| `plan_version` | 大于 | 输入 | 数字 `0` |

#### 查询结果字段

- `plan_status`
- `plan_confirmed`
- `plan_version`

不读取 `plan_draft`。

#### 业务作用

`plan_confirmed` 是 WF-07 首次生成任务的优先依据。只有它是可解析的非空对象时，才能作为已确认主规划使用；没有可用主规划时，后续仍可参考画像生成临时任务清单。

#### 禁止事项与易错点

1. `plan_version > 0` 只表示记录存在，不表示主规划已确认。
2. 是否可用必须由后面的大模型检查非空 `plan_confirmed`。
3. 不得读取或修改 `plan_draft`。
4. 不得增加“必须先完成 WF-06”的分支。
5. 查不到主规划也必须继续。
6. 本节点只查询，不写入任何数据。
7. 默认输出不得修改。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 上游连接 `WF07_DB01_读取画像状态`。
- [ ] 数据表选择 `wf06_main_plan_records`。
- [ ] 处理模式为查询数据。
- [ ] 查询上限为 1，排序留空。
- [ ] 数据范围为 `plan_version > 0`。
- [ ] 只读取 status、confirmed 和 version。
- [ ] 没有读取 draft。
- [ ] 默认输出保持不变。
- [ ] 没有增加主规划前置门槛。

### 第 3 个节点：`WF07_DB03_读取任务状态`

#### 前后连线

```text
WF07_DB02_读取主规划状态
→ WF07_DB03_读取任务状态
```

本节点完成后先停止；下一节点单独讨论。

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf07_semester_task_records` |
| 处理模式 | 查询数据 |
| 查询上限 | `1` |
| 排序 | 留空 |
| 输出 | 保持默认的 `isSuccess`、`message`、`outputList` |

#### 设置数据范围

| 表字段 | 条件 | 值类型 | 比较值 |
|---|---|---|---|
| `task_version` | 大于 | 输入 | 数字 `0` |

#### 查询结果字段

- `task_status`
- `task_draft`
- `task_confirmed`
- `task_version`
- `last_user_intent`
- `warnings`

六个业务字段必须全部读取。

#### 业务作用

本节点只探测当前用户是否已有 WF-07 模块记录，并把整个查询结果数组交给下一个“任务状态是否存在”分支器。它不负责判断任务是否已完成，也不是大模型的最终统一输入。

#### 禁止事项与易错点

1. 六个字段必须全部读取，不能只读 `task_status`。
2. `task_version > 0` 判断记录是否存在，不判断任务完成状态。
3. 不得在本节点增加 `task_status` 条件。
4. 不得读取系统自动字段。
5. 本节点只查询，不新增或更新数据。
6. 默认输出不得修改。
7. 后续分支器必须引用本节点的整个 `outputList`。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 上游连接 `WF07_DB02_读取主规划状态`。
- [ ] 数据表选择 `wf07_semester_task_records`。
- [ ] 处理模式为查询数据。
- [ ] 查询上限为 1，排序留空。
- [ ] 数据范围为 `task_version > 0`。
- [ ] 六个业务字段全部读取。
- [ ] 没有读取系统自动字段。
- [ ] 默认输出保持不变。
- [ ] 后续预留连接存在性分支器。

### 第 4 个节点：`WF07_任务状态是否存在`

#### 连线

```text
WF07_DB03_读取任务状态
→ WF07_任务状态是否存在
```

两个出口预留如下：

```text
如果（已有记录）
→ WF07_DB05_读取当前任务状态
```

```text
否则（没有记录）
→ WF07_DB04_初始化任务状态
→ WF07_DB05_读取当前任务状态
```

#### 分支器配置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 分支器 |
| 引用变量 | `WF07_DB03_读取任务状态.outputList` |
| 判断方式 | 数组长度 |
| 条件 | 大于 |
| 比较值类型 | Integer / 数字 |
| 比较值 | `0` |

#### 业务作用

本节点只判断 WF-07 模块记录是否存在。有记录时直接进入后面的正式统一读取；没有记录时先新增完整初始记录，再进入同一个正式读取节点。

#### 禁止事项与易错点

1. 必须判断整个 `outputList` 的长度，不得判断 `task_status`。
2. 比较值必须是数字 `0`，不能是 String `"0"`。
3. “如果”和“否则”不能接反。
4. 记录存在不等于任务已确认或模块已完成。
5. 两条路径最终必须汇合到同一个 DB05。
6. 分支器本身不修改任何数据。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 上游连接 `WF07_DB03_读取任务状态`。
- [ ] 引用 DB03 的整个 `outputList`。
- [ ] 判断方式为数组长度大于数字 `0`。
- [ ] “如果”出口预留给 DB05。
- [ ] “否则”出口预留给 DB04。
- [ ] 没有判断任务状态或完成状态。
- [ ] 两个出口没有接反。

### 第 5 个节点：`WF07_DB04_初始化任务状态`

#### 连线

```text
WF07_任务状态是否存在.否则
→ WF07_DB04_初始化任务状态
→ WF07_DB05_读取当前任务状态
```

“如果”出口不经过本节点，直接进入 DB05。

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf07_semester_task_records` |
| 处理模式 | 新增数据 |
| 输出 | 保持默认的 `isSuccess`、`message`、`outputList` |

#### 新增数据字段

| 字段名 | 值类型 | 填写值 | 字段类型 |
|---|---|---|---|
| `task_status` | 输入 | `not_started` | String |
| `task_draft` | 输入 | `{}` | String |
| `task_confirmed` | 输入 | `{}` | String |
| `task_version` | 输入 | 数字 `1` | Integer |
| `last_user_intent` | 输入 | `未记录` | String |
| `warnings` | 输入 | `[]` | String |

系统自动生成的 `id`、`uid`、`create_time` 等字段不填写。

#### 业务作用

首次进入 WF-07 且没有模块记录时，为当前用户建立一条完整初始记录。初始化完成后必须进入 DB05 重新查询，不能直接连接大模型。

#### 禁止事项与易错点

1. 只能连接存在性分支器的“否则”出口。
2. 处理模式必须是新增数据。
3. `{}` 和 `[]` 是手动输入的 String 内容。
4. `task_version` 必须是 Integer `1`，不能是 String。
5. 六个业务字段必须一次填齐。
6. 不得填写系统自动字段。
7. 初始化后必须进入 DB05 重新读取。
8. 不得直接连接大模型。
9. “如果”出口不能经过本节点。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 节点连接在“否则”出口。
- [ ] 数据表选择 `wf07_semester_task_records`。
- [ ] 处理模式为新增数据。
- [ ] 六个字段全部填写。
- [ ] draft 和 confirmed 为 String `{}`。
- [ ] warnings 为 String `[]`。
- [ ] version 为 Integer `1`。
- [ ] 没有填写系统自动字段。
- [ ] 后续连接 DB05。

## 九、下一步

下一次只讨论第 6 个节点：`WF07_DB05_读取当前任务状态`。该节点确认前不提前归档具体配置。
