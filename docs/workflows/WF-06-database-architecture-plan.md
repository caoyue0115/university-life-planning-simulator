# WF-06 主规划：数据库架构规划记录

> 分支：`wf03-wf12-architecture-planning`
>
> 工作方式：一次只讨论并确认一个节点。当前阶段冻结可实现的表结构、输入输出、代码、分支和连线，但暂不要求在讯飞星辰平台创建数据表或完成变量绑定。
>
> 资料基线：以用户本次提供的 WF-06 系统提示词、用户提示词和大模型节点截图为唯一业务底稿；数据库化只迁移状态读写方式，不擅自删改原业务规则。

## 阅读与执行方式

本文供后续负责讯飞星辰平台配置的同学直接执行。每个节点确认后，必须记录：节点名称、前后连线、节点设置、数据范围或输入、字段或输出、业务作用、禁止事项与易错点、完成检查。

文档状态必须区分：

- **已确认设计**：已经与用户讨论通过，可以按文档实施。
- **草案待确认**：仅供讨论，不能直接建表或绑定变量。
- **平台已验证**：已经在讯飞星辰实际运行并通过测试。

除非明确标记“平台已验证”，不得把规划描述成已经在画布完成。

## 一、已确认的业务基线

WF-06 把用户选定的路径转换成四层可执行计划：

```text
路径
→ 学期目标
→ 月度里程碑
→ 最近四周行动
```

主规划必须实际向用户展示：主路径和备选路径、本学期 3～4 个核心目标、关键里程碑、最近四周逐周行动、主要风险及应对措施，并在最后询问确认或修改。不能只回复“规划已经生成”或“等待确认”。

计划必须按年级适配：

- 大一：适应、探索、建立习惯；
- 大二：收敛 1～2 条路径并补核心能力；
- 大三：聚焦目标并建立备选；
- 大四：完成申请、求职、考试、毕业和成果整理。

success criteria 必须可观察；每个目标都要有资源、风险、替代方案和不做清单；最近四周只安排最小可执行行动。

## 二、上游信息优先级与边界

| 优先级 | 数据来源 | 处理方式 |
|---:|---|---|
| 1 | WF-04 `recommendation_confirmed` | 使用主路径和备选路径生成个性化规划 |
| 2 | WF-01 `profile_confirmed` | WF-04 为空时生成通用适应性规划，并在 warnings 说明缺少路径推荐结果 |
| 3 | 两者均为空 | 只能生成谨慎通用规划或请求目标信息，不能编造年级、专业或路径 |

固定规则：

1. WF-04 是优先输入，不是强制入口门槛。
2. WF-01 也不能成为进入 WF-06 的强制门槛。
3. 不得修改任何上游 confirmed 数据。
4. 用户明确想换路径时，进入 collecting；如有 WF-04 已确认结果则列出可选路径，否则询问用户目标，确认目标后再生成完整规划。

## 三、已确认的状态机

| 状态或动作 | 处理结果 |
|---|---|
| `collecting` | 用户想换路径但目标尚未确定，等待目标选择 |
| `awaiting_confirmation` | 完整规划已生成，等待确认或修改 |
| `confirm` | 仅在待确认且 draft 非空时复制 draft 到 confirmed、清空 draft、状态变为 `complete`、按需加入 WF-06、`next_workflow=WF-12` |
| `modify` | 基于现有 draft 或 confirmed 创建修订草稿，说明影响，不得偷偷覆盖 confirmed |
| `cancel` | 清空 draft、保留 confirmed，状态变为 `cancelled` |
| `clarification_needed` | 没有可确认或可修改内容，或缺少生成计划所需目标信息 |

## 四、draft 结构基线与原提示词解释

```json
{
  "selected_route":"",
  "alternative_route":"",
  "target_route":"",
  "planning_horizon":"",
  "semester_goals":[
    {
      "学期":"",
      "进球":"",
      "success_criteria":[],
      "monthly_milestones":[],
      "资源":[],
      "风险":[],
      "后备":[],
      "not_to_do":[]
    }
  ],
  "next_four_weeks":[
    {"week":1,"actions":[],"evidence":[]}
  ],
  "decision_points":[],
  "摘要":""
}
```

为保持原始字段合同并确保可实现，现阶段固定采用以下解释：

1. 保留历史键 `进球`，解释为“本学期核心目标”，不在未确认时擅自改名。
2. 原提示词要求保存的“里程碑”对应 `semester_goals[].monthly_milestones`。
3. 原提示词要求保存的“风险”对应 `semester_goals[].风险`。
4. `modify` 中“基于现有 draft 或 confirm”解释为“基于现有 draft 或 confirmed”，不把 confirm 动作当成数据对象。
5. 不新增重复的顶层“里程碑”或“风险”字段。

## 五、当前大模型节点基线

| 配置项 | 当前值 |
|---|---|
| 当前输入 | `user_input`、`router_context` |
| 当前输出格式 | text |
| 当前唯一输出 | `output`，String |

数据库化后预计增加：

- `profile_state_rows`
- `recommendation_state_rows`
- `plan_state_rows`

输出仍保持单一 `output:String`。正式迁移时以用户提供的原提示词为底稿做最小修改，保留四层计划、年级适配、确认文案、修改、取消、容量规则和实际展示要求，只把完整 prior state 改为数据库输入与增量输出。

## 六、WF-06 模块表字段基线

建议表名：`wf06_main_plan_records`

| 字段 | 类型 | 默认值 | 用途 |
|---|---|---|---|
| `plan_status` | String | `not_started` | WF-06 当前业务状态 |
| `plan_draft` | String | `{}` | 收集中的路径选择或待确认主规划 |
| `plan_confirmed` | String | `{}` | 用户确认后的主规划 |
| `plan_version` | Integer | `1` | 模块记录版本 |
| `last_user_intent` | String | `未记录` | 用户本轮原话 |
| `warnings` | String | `[]` | 本模块警告数组的 JSON 字符串 |

这六个字段已经作为后续节点的固定合同。后续读取、初始化、重新读取、大模型输入、C01 输出和模块写回都必须使用同一组字段，不得中途增加遗漏字段。

draft、confirmed 和 warnings 的真实内容不是在建表时展开填写。大模型生成对象或数组，C01 序列化为 String，模块数据库更新节点再写入。回复不写数据库，继续通过共享 `last_reply`、现有 N90 和唯一结束节点返回。

当前状态：业务基线、上游优先级、状态机、结构解释和六字段模块表基线已经讨论确认并归档；讯飞星辰平台尚未实际建表。

## 七、预期主链路骨架

```text
M03 MAIN 总分支器的 WF-06 出口
→ 读取 WF-01 已确认画像
→ 读取 WF-04 已确认路径推荐
→ 读取 WF-06 主规划状态
→ 判断记录是否存在
→ 必要时初始化
→ 统一重新读取 WF-06 当前状态
→ WF-06 大模型
→ C01 解析模型输出
→ 解析是否成功
→ 更新 WF-06 模块表
→ C02 合并公共路由状态
→ 更新公共路由表
→ 写入共享 last_reply
→ 现有 N90
→ 唯一结束节点
```

本节只冻结总体方向，不代表所有节点配置已经确认。后续仍按“一次一个节点、用户确认后立即归档推送”的方式推进。

## 八、逐节点确认记录

### 第 1 个节点：`WF06_DB01_读取画像状态`

#### 前后连线

```text
M03 MAIN 总分支器的 WF-06 出口
→ WF06_DB01_读取画像状态
```

本节点完成后先停止；下一节点单独讨论。

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf01_profile_records` |
| 处理模式 | 查询数据 |
| 查询上限 | `1` |
| 排序 | 留空 |
| 输出 | 保持默认的 `isSuccess`、`message`、`outputList` |

#### 设置数据范围

| 表字段 | 条件 | 值类型 | 比较值 |
|---|---|---|---|
| `profile_version` | 大于 | 输入 | `0` |

#### 查询结果字段

- `profile_status`
- `profile_confirmed`
- `profile_version`

不读取 `profile_draft`。

#### 业务作用

当 WF-04 没有已确认路径推荐时，WF-06 可以使用可解析的非空 `profile_confirmed` 中的年级、专业和目标生成通用适应性规划。没有画像、画像为空或无法解析时，不能强制返回 WF-01；后续只能谨慎生成通用规划或询问规划目标，不能编造画像。

#### 禁止事项与易错点

1. `profile_version > 0` 只表示记录存在，不表示画像已确认。
2. 是否可用必须由后面的大模型检查非空 `profile_confirmed`。
3. 不得读取或修改 `profile_draft`。
4. 不得增加“无画像返回 WF-01”的分支。
5. 本节点只查询，不写入任何数据。
6. 不得误选 WF-06 自身模块表。
7. 默认输出不得修改。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 节点名称为 `WF06_DB01_读取画像状态`。
- [ ] 节点连接在 M03 的 WF-06 出口之后。
- [ ] 数据表选择 `wf01_profile_records`。
- [ ] 数据范围为 `profile_version > 0`。
- [ ] 查询上限为 `1`。
- [ ] 只勾选 `profile_status`、`profile_confirmed`、`profile_version`。
- [ ] 没有画像硬门槛。
- [ ] 默认输出保持不变。

### 第 2 个节点：`WF06_DB02_读取五路径推荐`

#### 前后连线

```text
WF06_DB01_读取画像状态
→ WF06_DB02_读取五路径推荐
```

本节点完成后先停止；下一节点单独讨论。

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf04_path_recommendation_records` |
| 处理模式 | 查询数据 |
| 查询上限 | `1` |
| 排序 | 留空 |
| 输出 | 保持默认的 `isSuccess`、`message`、`outputList` |

#### 设置数据范围

| 表字段 | 条件 | 值类型 | 比较值 |
|---|---|---|---|
| `recommendation_version` | 大于 | 输入 | `0` |

#### 查询结果字段

- `recommendation_status`
- `recommendation_confirmed`
- `recommendation_version`

不读取 `recommendation_draft`。

#### 业务作用

WF-06 优先使用已确认的 WF-04 推荐结果：`primary_route` 作为主路径，`alternative_routes` 作为备选路径，五路径中的证据、空档、风险和行动建议可作为规划依据。查询不到记录或 confirmed 为空时仍继续，回退使用 WF-01 已确认画像，并在 warnings 说明缺少路径推荐结果。

#### 禁止事项与易错点

1. `recommendation_version > 0` 只表示记录存在，不表示推荐已确认。
2. 后续大模型必须检查非空 `recommendation_confirmed`。
3. 不得读取或修改 `recommendation_draft`。
4. 不得增加“没有 WF-04 就返回 WF-04”的分支。
5. 本节点只查询，不写入数据。
6. 不得误选 WF-06 自身主规划表。
7. 默认输出不得修改。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 上游连接 `WF06_DB01_读取画像状态`。
- [ ] 数据表选择 `wf04_path_recommendation_records`。
- [ ] 数据范围为 `recommendation_version > 0`。
- [ ] 查询上限为 `1`。
- [ ] 只勾选 `recommendation_status`、`recommendation_confirmed`、`recommendation_version`。
- [ ] 没有读取 draft。
- [ ] 没有增加 WF-04 前置门槛。
- [ ] 默认输出保持不变。

### 第 3 个节点：`WF06_DB03_读取主规划状态`

#### 前后连线

```text
WF06_DB02_读取五路径推荐
→ WF06_DB03_读取主规划状态
```

本节点之后才连接“主规划状态是否存在”分支器。

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf06_main_plan_records` |
| 处理模式 | 查询数据 |
| 查询上限 | `1` |
| 排序 | 留空 |
| 输出 | 保持默认的 `isSuccess`、`message`、`outputList` |

这张表当前只做架构设计，暂时不要求在讯飞星辰平台实际创建。

#### 设置数据范围

| 表字段 | 条件 | 值类型 | 比较值 |
|---|---|---|---|
| `plan_version` | 大于 | 输入 | `0` |

#### 查询结果字段

- `plan_status`
- `plan_draft`
- `plan_confirmed`
- `plan_version`
- `last_user_intent`
- `warnings`

#### 业务作用

1. 首次进入时查询结果为空，后续分支器据此进入初始化路径。
2. 后续进入时恢复收集中的路径选择、待确认计划或已确认计划。
3. 用户确认、修改、取消或继续选择目标路径时，大模型依靠这些字段恢复当前状态。

#### 禁止事项与易错点

1. 本节点只能查询，不能新增或更新。
2. `plan_version > 0` 只判断记录存在，不表示规划已完成。
3. 六个字段必须一次选齐。
4. 不在本节点填写 draft、confirmed 的真实内容。
5. 不得使用 WF-04 的状态代替 WF-06 自身状态。
6. 不得遗漏 `last_user_intent` 或 `warnings`。
7. 本节点之后才连接记录存在性分支器。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 上游连接 `WF06_DB02_读取五路径推荐`。
- [ ] 数据表选择 `wf06_main_plan_records`。
- [ ] 数据范围为 `plan_version > 0`。
- [ ] 查询上限为 `1`。
- [ ] 六个业务字段全部选择。
- [ ] 处理模式为查询数据。
- [ ] 默认输出保持不变。
- [ ] 后续预留连接“主规划状态是否存在”分支器。

### 第 4 个节点：`WF06_主规划状态是否存在`

#### 连线

```text
WF06_DB03_读取主规划状态
→ WF06_主规划状态是否存在
```

两个出口：

```text
如果：已有 WF-06 记录
→ WF06_DB05_读取当前主规划状态

否则：没有 WF-06 记录
→ WF06_DB04_初始化主规划状态
```

DB04 初始化完成后也进入 DB05。

#### 分支器配置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 分支器 |
| 引用变量 | `WF06_DB03_读取主规划状态.outputList` |
| 条件 | 长度大于 |
| 比较值类型 | 输入 |
| 比较值 | 数字 `0` |

#### 两个出口的准确含义

| 出口 | 含义 | 后续节点 |
|---|---|---|
| 如果 | `outputList` 长度大于 `0`，已有 WF-06 记录 | DB05 统一重读 |
| 否则 | 查询结果为空，首次进入 WF-06 | DB04 初始化后进入 DB05 |

#### 业务作用

本节点只判断 WF-06 模块记录是否存在，不判断主规划是否完成、是否有待确认草稿、是否正在收集目标路径，也不判断 WF-01 或 WF-04 是否完成。业务状态由后续大模型处理。

#### 禁止事项与易错点

1. 必须引用 DB03 的整个 `outputList`。
2. 条件必须是长度大于数字 `0`。
3. “如果”和“否则”不能接反。
4. 不得使用 `plan_status` 判断记录是否存在。
5. 不得增加画像或五路径推荐的完成门槛。
6. 两个出口后续都必须连接。
7. 数据库查询失败不能等同于查询成功但结果为空；整链验收时单独核对。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 上游连接 `WF06_DB03_读取主规划状态`。
- [ ] 引用 DB03 的整个 `outputList`。
- [ ] 条件为长度大于数字 `0`。
- [ ] “如果”出口预留给 DB05。
- [ ] “否则”出口预留给 DB04。
- [ ] 没有判断业务完成状态。
- [ ] 没有增加上游模块前置门槛。
- [ ] 两个出口没有接反。

### 第 5 个节点：`WF06_DB04_初始化主规划状态`

#### 连线

```text
WF06_主规划状态是否存在.否则
→ WF06_DB04_初始化主规划状态
→ WF06_DB05_读取当前主规划状态
```

“如果”出口不经过本节点，直接进入 DB05。

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf06_main_plan_records` |
| 处理模式 | 新增数据 |
| 输出 | 保持默认的 `isSuccess`、`message`、`outputList` |

#### 新增数据字段

六个业务字段必须逐项填写：

| 字段名 | 值类型 | 填写值 | 字段类型 |
|---|---|---|---|
| `plan_status` | 输入 | `not_started` | String |
| `plan_draft` | 输入 | `{}` | String |
| `plan_confirmed` | 输入 | `{}` | String |
| `plan_version` | 输入 | `1` | Integer |
| `last_user_intent` | 输入 | `未记录` | String |
| `warnings` | 输入 | `[]` | String |

系统自动生成的 `id`、`uid`、`create_time` 等字段不填写。

#### 业务作用

首次进入 WF-06 且没有主规划记录时，为当前用户建立完整初始记录。初始化完成后必须进入 DB05 重新查询，不能直接连接大模型。

#### 禁止事项与易错点

1. 只能连接存在性分支器的“否则”出口。
2. 处理模式必须是新增数据。
3. `{}` 和 `[]` 是手动输入的 String 内容。
4. `plan_version` 必须是 Integer `1`，不是 String。
5. 六个业务字段必须一次填齐。
6. 不得填写系统自动字段。
7. 初始化后必须进入 DB05 重新读取。
8. 不得直接连接大模型。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 节点连接在“否则”出口。
- [ ] 数据表选择 `wf06_main_plan_records`。
- [ ] 处理模式为新增数据。
- [ ] 六个字段全部填写。
- [ ] draft 和 confirmed 为 String `{}`。
- [ ] warnings 为 String `[]`。
- [ ] version 为 Integer `1`。
- [ ] 没有填写系统字段。
- [ ] 后续连接 DB05。

### 第 6 个节点：`WF06_DB05_读取当前主规划状态`

本节点可直接复制第 3 个节点 `WF06_DB03_读取主规划状态`，再修改节点名称。

#### 两条入口连线

```text
WF06_主规划状态是否存在.如果
→ WF06_DB05_读取当前主规划状态
```

```text
WF06_主规划状态是否存在.否则
→ WF06_DB04_初始化主规划状态
→ WF06_DB05_读取当前主规划状态
```

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf06_main_plan_records` |
| 处理模式 | 查询数据 |
| 查询上限 | `1` |
| 排序 | 留空 |
| 输出 | 保持默认的 `isSuccess`、`message`、`outputList` |

#### 设置数据范围

| 表字段 | 条件 | 值类型 | 比较值 |
|---|---|---|---|
| `plan_version` | 大于 | 输入 | `0` |

#### 查询结果字段

- `plan_status`
- `plan_draft`
- `plan_confirmed`
- `plan_version`
- `last_user_intent`
- `warnings`

#### 后续连接

```text
WF06_DB05_读取当前主规划状态
→ WF06_LLM_主规划
```

大模型的输入和提示词在下一节点单独讨论，本步骤不提前修改。

#### 业务作用

DB03 只探测记录是否存在；DB05 是两条路径汇合后的正式统一读取。无论用户已有记录，还是本轮刚初始化，大模型都只引用 DB05 的输出，不需要区分数据来自 DB03 还是 DB04。

#### 禁止事项与易错点

1. DB05 必须有两条入口。
2. 配置必须与 DB03 完全一致。
3. DB04 后不能绕过 DB05 直接连接大模型。
4. 大模型后续必须引用 DB05，不再引用 DB03。
5. 六个查询字段必须全部选择。
6. 处理模式必须是查询数据。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] DB05 由 DB03 复制得到。
- [ ] 节点名称修改为 `WF06_DB05_读取当前主规划状态`。
- [ ] “如果”出口直接连接 DB05。
- [ ] “否则”出口经过 DB04 后连接 DB05。
- [ ] 查询范围与 DB03 完全一致。
- [ ] 六个查询字段全部选择。
- [ ] 后续预留连接 WF-06 大模型。

### 第 7 个节点：`WF06_LLM_主规划`

#### 连线与节点设置

```text
WF06_DB05_读取当前主规划状态
→ WF06_LLM_主规划
→ WF06_C01_解析模型输出
```

保持当前模型，关闭对话历史；输出格式为 text，唯一输出为 `output:String`。

#### 输入

| 参数名 | 引用来源 |
|---|---|
| `user_input` | `开始.AGENT_USER_INPUT` |
| `router_context` | `N01 路由大模型.output` |
| `profile_state_rows` | `WF06_DB01_读取画像状态.outputList` |
| `recommendation_state_rows` | `WF06_DB02_读取五路径推荐.outputList` |
| `plan_state_rows` | `WF06_DB05_读取当前主规划状态.outputList` |

#### 完整提示词

系统提示词、用户提示词及整体复制文本单独保存在：[WF-06 数据库版大模型提示词](prompts/WF-06-database-llm-prompts.md)。执行同学必须整体复制，不得继续使用旧版完整 state 提示词。

#### 已确认边界

- 原始四层规划、年级适配、3～4 个核心目标、可观察标准、资源、风险、后备、不做清单和四周行动均保留。
- 优先使用 WF-04 confirmed，缺失时回退 WF-01 confirmed；两者缺失时不编造，必要时进入 collecting。
- 用户想换路径时先收集目标，不能擅自生成未经确认的新路径规划。
- `进球`、嵌套里程碑和风险、`confirm/confirmed` 歧义按基线解释固定处理。
- 模型只输出十个增量字段，不输出完整 state 或 completed_workflows。
- 确认原文、修改、取消、实际回复展示要求和容量规则均保留。
- 输出仍为单一 String `output`。

#### 当前状态

- 输入、输出和完整提示词：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

## 九、下一步

下一次只讨论第 8 个节点：`WF06_C01_解析模型输出`。代码、输入、输出和测试结果在确认前不归档推送。
