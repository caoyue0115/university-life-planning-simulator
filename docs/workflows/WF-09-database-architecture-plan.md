# WF-09 履历素材：数据库架构规划记录

> 分支：`wf03-wf12-architecture-planning`
>
> 工作方式：每次仍只向用户提交一个节点确认；后台可提前准备和测试后续模板。用户确认一个节点后，立即归档、推送、核对远端并给出下一个节点。
>
> 资料基线：以用户本次提供的 WF-09 系统提示词和大模型节点截图为业务底稿；数据库化只迁移状态读写方式，不擅自增加经历事实或改变确认规则。

## 阅读与执行方式

本文供后续负责讯飞星辰平台配置的同学直接执行。每个节点确认后，必须记录：节点名称、前后连线、节点设置、数据范围或输入、字段或输出、业务作用、禁止事项与易错点、完成检查。

文档状态必须区分：

- **已确认设计**：已经与用户讨论通过，可以按文档实施。
- **草案待确认**：仅供讨论，不能直接建表或绑定变量。
- **平台已验证**：已经在讯飞星辰实际运行并通过测试。

除非明确标记“平台已验证”，不得把规划描述成已经在画布完成。

## 一、业务职责与事实边界

WF-09 把用户真实提供的经历整理成履历条目。模块可以改善结构和表达，但绝对不能补造行动、结果、数字、奖项、工具或证明材料。

固定规则：

1. 用户本轮原话、WF-01 已确认画像、WF-07 已确认任务证据、WF-08 已确认复盘证据和 WF-09 历史确认条目是本模块可读取的数据来源。
2. WF-09 没有强制上游工作流；任何上游记录缺失都不能阻止用户直接进入 WF-09。
3. WF-01 画像只能用于年级、专业、学校等背景，不得作为用户完成某项经历的证据。
4. WF-07 任务只有在状态为完成且包含用户提供的 `actual_evidence` 时，才可作为履历事实来源；待处理或推迟任务不能当成已完成经历。
5. WF-08 复盘只能使用 `explicit_new_facts` 和 `behavior_evidence`；`agent_inferences`、建议或待验证问题不能当作履历事实。
6. WF-06 主规划、WF-04 路径推荐以及其他未来规划均不读取为事实来源，不能把“计划做”当成“已经做”。
7. 用户要求夸大、伪造或冒领时必须拒绝，并帮助其基于真实内容改写。
8. 没有数字时 `指标=[]`；没有证明位置时 `evidence_locations=[]`，且质量状态至少为“缺少证明材料”。
9. 缺少关键事实时进入 collecting，每轮最多询问三个缺口，不生成完整履历条目。

## 二、关键事实门禁

生成完整条目前至少需要：

- 经历类型；
- 背景或目标；
- 用户本人角色或职责；
- 用户本人采取的行动。

结果、数字和证明材料不是生成草稿的绝对前置，但缺失时必须如实保留空数组并降低 `quality_status`，不得用常识补齐。

## 三、结构歧义的统一解释

原提示词存在三处需要在数据库版中固定的文字歧义：

1. confirmed 示例使用中文“条目”，确认规则写成 `confirmed.entrys`。数据库版统一使用正确英文键 `entries`，禁止再使用 `entrys` 或中文“条目”作为结构键。
2. 单条履历结构中的中文键“进球”按上下文明确解释为“目标”，数据库版统一使用中文键 `目标`。
3. 模块完成状态统一使用英文 `complete`；单条履历的质量状态仍使用原提示词规定的中文枚举。

这些调整只消除键名冲突，不改变业务含义。

## 四、单条履历结构基线

```json
{
  "entry_id":"R01",
  "entry_type":"课程项目|科研|竞赛|实习|社团|学生工作|志愿服务|其他",
  "背景":"",
  "目标":"",
  "角色":"",
  "动作":[],
  "工具":[],
  "结果":[],
  "指标":[],
  "evidence_locations":[],
  "resume_bullet":"",
  "detailed_story":"",
  "quality_status":"可直接使用|缺少量化结果|缺少证明材料|需要打磨",
  "missing_fields":[]
}
```

固定约束：

1. `entry_id` 在当前 confirmed entries 与 pending entry 中唯一，格式使用 `R01`、`R02` 等。
2. `resume_bullet` 使用“行动—方法—结果”结构，但只能写已有依据的结果。
3. `detailed_story` 只整理真实背景、行动、困难和结果。
4. 普通数组最多 5 项；confirmed 中 `entries` 最多保留 5 条，后续若确需更多容量再单独讨论，当前不增加数据库列。

## 五、draft 与 confirmed 结构基线

draft：

```json
{
  "pending_entry":{},
  "existing_entries":[],
  "摘要":""
}
```

confirmed：

```json
{
  "entries":[],
  "摘要":""
}
```

首次收集信息时可以在 `pending_entry` 中保存已经确认的部分事实和 `missing_fields`，但不能提前生成完整 `resume_bullet` 并进入 awaiting_confirmation。

## 六、状态机与动作规则

模块状态统一使用：

- `not_started`
- `collecting`
- `awaiting_confirmation`
- `complete`
- `cancelled`
- 明确澄清或安全拒绝时允许 `current_status="clarification_needed"`

动作规则：

1. 信息不足：保存部分事实草稿，status=collecting，最多追问三个缺口。
2. 信息完整：生成完整 pending entry，status=awaiting_confirmation。
3. confirm：仅当 pending entry 非空且完整时，将其追加到 confirmed.entries，保证 entry_id 唯一，清空 draft，status=complete，追加完成标记 WF-09，next_workflow=WF-12。
4. modify：只修正用户明确指出的事实或表达，不改变未被指出的事实。
5. cancel：只清空当前 draft，保留 confirmed.entries，status=cancelled。
6. 伪造请求：拒绝伪造，不把虚假内容保存为 draft 或 confirmed。

## 七、当前大模型节点基线

| 配置项 | 当前值 |
|---|---|
| 当前输入 | `user_input`、`router_context` |
| 当前输出格式 | text |
| 当前唯一输出 | `output`，String |
| 当前模型 | Kimi-K2.5 |
| 对话历史 | 关闭 |

数据库化后预计增加：

- `profile_state_rows`
- `task_state_rows`
- `review_state_rows`
- `resume_state_rows`

输出仍保持单一 `output:String`。正式迁移时以用户提供的原提示词为底稿做最小修改，只把完整 prior state 改为六个明确输入与增量输出，并保留事实门禁、反伪造、确认、修改和取消规则。上游输入都是可选增强，空数组时仍须继续运行。

## 八、WF-09 模块表字段基线

建议表名：`wf09_resume_asset_records`

| 字段 | 类型 | 默认值 | 用途 |
|---|---|---|---|
| `asset_status` | String | `not_started` | WF-09 当前模块状态 |
| `asset_draft` | String | `{}` | collecting 或 awaiting_confirmation 草稿 |
| `asset_confirmed` | String | `{}` | 用户已确认的履历条目集合 |
| `asset_version` | Integer | `1` | 模块记录版本 |
| `last_user_intent` | String | `未记录` | 用户本轮原话 |
| `warnings` | String | `[]` | 本模块警告数组的 JSON 字符串 |

六个字段是后续读取、初始化、统一重新读取、大模型输入、C01 输出和模块写回的固定合同。单条履历的所有业务字段都保存在 draft 或 confirmed JSON 中，不再拆成数据库列。

回复不写数据库，继续通过共享 `last_reply`、现有 N90 和唯一结束节点返回。

当前状态：业务基线、事实门禁、结构解释、状态机和六字段模块表已经整理；讯飞星辰平台尚未实际建表。

## 九、预期主链路骨架

```text
M03 MAIN 总分支器的 WF-09 出口
→ 读取 WF-01 已确认画像
→ 读取 WF-07 已确认任务证据
→ 读取 WF-08 已确认复盘证据
→ 读取 WF-09 履历素材状态
→ 判断记录是否存在
→ 必要时初始化
→ 统一重新读取 WF-09 当前状态
→ WF-09 大模型
→ C01 解析模型输出
→ 解析是否成功
→ 更新 WF-09 模块表
→ C02 合并公共路由状态
→ 更新公共路由表
→ 写入共享 last_reply
→ 现有 N90
→ 唯一结束节点
```

完整链路预计为 14 个新节点；仍逐个确认，不打包确认。减少展示篇幅不能省略依赖检查、用户确认、代码测试或远端核对。

## 十、逐节点确认记录

此前曾错误地从 WF-09 自身状态读取开始，并提前归档三个节点；这些节点记录已通过后续纠正提交从现行文档撤销，不得按旧提交搭建。

### 第 1 个节点：`WF09_DB01_读取画像状态`

#### 前后连线

```text
M03 MAIN 总分支器的 WF-09 出口
→ WF09_DB01_读取画像状态
→ WF09_DB02_读取学期任务状态
```

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf01_profile_records` |
| 处理模式 | 查询数据 |
| 查询上限 | `1` |
| 排序 | 留空 |
| 输出 | 保持默认的 `isSuccess`、`message`、`outputList` |

#### 设置数据范围

| 表字段 | 条件 | 值类型 | 比较值 |
|---|---|---|---|
| `profile_version` | 大于 | 输入 | 数字 `0` |

#### 查询结果字段

- `profile_status`
- `profile_confirmed`
- `profile_version`

不读取 `profile_draft`。

#### 业务作用

已确认画像只用于学校、年级、专业等背景。它不能证明用户完成过某项经历；查询为空时 WF-09 仍继续运行。

#### 禁止事项与易错点

1. 不得增加“无画像返回 WF-01”的分支。
2. `profile_version > 0` 只表示存在记录，不表示画像已确认。
3. 不得读取或修改 `profile_draft`。
4. 本节点只查询，不写入任何数据。
5. 不得把画像中的目标、能力或偏好当作履历事实。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 节点连接在 MAIN 的 WF-09 出口之后。
- [ ] 数据表选择 `wf01_profile_records`。
- [ ] 数据范围为 `profile_version > 0`。
- [ ] 只读取 status、confirmed 和 version。
- [ ] 没有读取 draft。
- [ ] 没有新增画像硬门槛。
- [ ] 后续连接 DB02。

## 十一、下一步

### 第 2 个节点：`WF09_DB02_读取学期任务状态`

#### 前后连线

```text
WF09_DB01_读取画像状态
→ WF09_DB02_读取学期任务状态
→ WF09_DB03_读取成长复盘状态
```

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf07_semester_task_records` |
| 处理模式 | 查询数据 |
| 查询上限 | `1` |
| 排序 | 留空 |
| 输出 | 保持默认的 `isSuccess`、`message`、`outputList` |

#### 设置数据范围

| 表字段 | 条件 | 值类型 | 比较值 |
|---|---|---|---|
| `task_version` | 大于 | 输入 | 数字 `0` |

#### 查询结果字段

- `task_status`
- `task_confirmed`
- `task_version`

不读取 `task_draft`。

#### 业务作用

读取 WF-07 已确认任务。后续大模型只能把状态为“完成”且含有用户实际结果或 `actual_evidence` 的任务作为履历事实；待处理、推迟、取消或只有计划的任务不能当作已完成经历。查询为空时 WF-09 仍继续运行。

#### 禁止事项与易错点

1. 不得读取或使用未确认 `task_draft`。
2. `task_version > 0` 只表示记录存在，不表示任务已完成。
3. 不得把 `expected_evidence` 当成 `actual_evidence`。
4. 不得把待处理、推迟或取消任务写成履历经历。
5. 不得增加“无任务返回 WF-07”的硬门槛。
6. 本节点只查询，不写入任何数据。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 上游连接 DB01。
- [ ] 数据表选择 `wf07_semester_task_records`。
- [ ] 数据范围为 `task_version > 0`。
- [ ] 只读取 status、confirmed 和 version。
- [ ] 没有读取 draft。
- [ ] 没有增加 WF-07 硬门槛。
- [ ] 后续连接 DB03。

### 第 3 个节点：`WF09_DB03_读取成长复盘状态`

#### 前后连线

```text
WF09_DB02_读取学期任务状态
→ WF09_DB03_读取成长复盘状态
→ WF09_DB04_读取履历素材状态
```

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf08_growth_review_records` |
| 处理模式 | 查询数据 |
| 查询上限 | `1` |
| 排序 | 留空 |
| 输出 | 保持默认的 `isSuccess`、`message`、`outputList` |

#### 设置数据范围

| 表字段 | 条件 | 值类型 | 比较值 |
|---|---|---|---|
| `review_version` | 大于 | 输入 | 数字 `0` |

#### 查询结果字段

- `review_status`
- `review_confirmed`
- `review_version`

不读取 `review_draft`。

#### 业务作用

读取 WF-08 已确认复盘。后续大模型只能把 `explicit_new_facts` 和 `behavior_evidence` 作为候选履历事实；`agent_inferences`、推荐、调整建议和待验证问题不能作为事实。查询为空时 WF-09 仍继续运行。

#### 禁止事项与易错点

1. 不得读取或使用未确认 `review_draft`。
2. `review_version > 0` 只表示记录存在，不表示复盘已确认。
3. 不得把 `agent_inferences` 当成用户事实。
4. 不得把 `recommended_adjustments` 或 `questions_to_verify` 写成已完成经历。
5. 不得增加“无复盘返回 WF-08”的硬门槛。
6. 本节点只查询，不写入任何数据。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 上游连接 DB02。
- [ ] 数据表选择 `wf08_growth_review_records`。
- [ ] 数据范围为 `review_version > 0`。
- [ ] 只读取 status、confirmed 和 version。
- [ ] 没有读取 draft。
- [ ] 没有增加 WF-08 硬门槛。
- [ ] 后续连接 DB04。

### 第 4 个节点：`WF09_DB04_读取履历素材状态`

#### 前后连线

```text
WF09_DB03_读取成长复盘状态
→ WF09_DB04_读取履历素材状态
→ WF09_履历素材状态是否存在
```

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf09_resume_asset_records` |
| 处理模式 | 查询数据 |
| 查询上限 | `1` |
| 排序 | 留空 |
| 输出 | 保持默认的 `isSuccess`、`message`、`outputList` |

#### 设置数据范围

| 表字段 | 条件 | 值类型 | 比较值 |
|---|---|---|---|
| `asset_version` | 大于 | 输入 | 数字 `0` |

#### 查询结果字段

- `asset_status`
- `asset_draft`
- `asset_confirmed`
- `asset_version`
- `last_user_intent`
- `warnings`

六个业务字段必须全部读取。

#### 业务作用

本节点只探测当前用户是否已有 WF-09 模块记录，并把整个查询结果数组交给存在性分支器。它不是大模型的最终统一输入，也不负责判断履历条目是否已确认。

#### 禁止事项与易错点

1. 必须读取六个字段，不能只读取 status 或 confirmed。
2. `asset_version > 0` 只表示记录存在，不表示模块已完成。
3. 本节点只查询，不新增或更新数据。
4. 下一分支器必须引用整个 `outputList`。
5. 大模型后续必须引用统一重读节点 DB06，不能引用本节点。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 上游连接 DB03。
- [ ] 数据表选择 `wf09_resume_asset_records`。
- [ ] 数据范围为 `asset_version > 0`。
- [ ] 六个业务字段全部读取。
- [ ] 默认输出保持不变。
- [ ] 后续连接存在性分支器。

### 第 5 个节点：`WF09_履历素材状态是否存在`

#### 连线

```text
WF09_DB04_读取履历素材状态
→ WF09_履历素材状态是否存在
```

#### 节点配置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 分支器 |
| 判断变量 | 引用 `WF09_DB04_读取履历素材状态.outputList` |
| 判断方式 | 数组长度大于 |
| 比较值类型 | Integer/数字 |
| 比较值 | `0` |

#### 两个出口

```text
如果（数组长度 > 0）
→ WF09_DB06_读取当前履历素材状态
```

```text
否则
→ WF09_DB05_初始化履历素材状态
→ WF09_DB06_读取当前履历素材状态
```

#### 业务作用

本分支器只判断 WF-09 模块记录是否存在。有记录时进入统一读取；没有记录时先初始化，再进入同一个统一读取节点。

#### 禁止事项与易错点

1. 必须判断 DB04 整个 `outputList` 的数组长度，不能判断 `asset_status`。
2. 比较值必须是数字 0，不能是字符串 `"0"`。
3. 不得把 `asset_version > 0` 解释为履历已经完成。
4. 否则出口必须先初始化，不能直接连接大模型或回复节点。
5. 两条路径最终必须汇入同一个 DB06。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 分支器引用 DB04 的整个 `outputList`。
- [ ] 条件为数组长度大于数字 0。
- [ ] 如果出口连接 DB06。
- [ ] 否则出口连接 DB05，DB05 后再连接 DB06。

### 第 6 个节点：`WF09_DB05_初始化履历素材状态`

#### 连线

```text
WF09_履历素材状态是否存在.否则
→ WF09_DB05_初始化履历素材状态
→ WF09_DB06_读取当前履历素材状态
```

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf09_resume_asset_records` |
| 处理模式 | 新增数据 |

#### 新增字段

| 表字段 | 值类型 | 填写值 |
|---|---|---|
| `asset_status` | 输入/String | `not_started` |
| `asset_draft` | 输入/String | `{}` |
| `asset_confirmed` | 输入/String | `{}` |
| `asset_version` | 输入/Integer | 数字 `1` |
| `last_user_intent` | 输入/String | `未记录` |
| `warnings` | 输入/String | `[]` |

#### 业务作用

只在当前用户不存在 WF-09 模块记录时新增一条完整初始记录，使后续统一读取节点始终获得同一套六字段结构。

#### 禁止事项与易错点

1. 本节点只能连接记录不存在的否则出口。
2. `{}` 和 `[]` 按 String 写入；`asset_version` 按 Integer 写入。
3. 不填写平台自动生成的系统字段。
4. 不得初始化旧版 `resume_entries` 表。
5. 新增后必须进入 DB06 统一重读，不能直接连接大模型。
6. 不得把初始 status 写成 `complete` 或 `collecting`。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 处理模式为新增数据。
- [ ] 数据表选择 `wf09_resume_asset_records`。
- [ ] 六个字段全部填写且类型正确。
- [ ] 只连接否则出口。
- [ ] 系统自动字段未填写。
- [ ] 后续连接 DB06。

### 第 7 个节点：`WF09_DB06_读取当前履历素材状态`

本节点可以直接复制第 4 个节点 `WF09_DB04_读取履历素材状态`，再修改节点名称。

#### 两条入口连线

```text
WF09_履历素材状态是否存在.如果
→ WF09_DB06_读取当前履历素材状态
```

```text
WF09_履历素材状态是否存在.否则
→ WF09_DB05_初始化履历素材状态
→ WF09_DB06_读取当前履历素材状态
```

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf09_resume_asset_records` |
| 处理模式 | 查询数据 |
| 查询上限 | `1` |
| 排序 | 留空 |
| 输出 | 保持默认的 `isSuccess`、`message`、`outputList` |

#### 设置数据范围

| 表字段 | 条件 | 值类型 | 比较值 |
|---|---|---|---|
| `asset_version` | 大于 | 输入 | 数字 `0` |

#### 查询结果字段

- `asset_status`
- `asset_draft`
- `asset_confirmed`
- `asset_version`
- `last_user_intent`
- `warnings`

六个业务字段必须全部读取。

#### 业务作用

DB04 只探测记录是否存在；DB06 是两条路径汇合后的正式统一读取。无论用户已有记录还是本轮刚初始化，大模型都只引用 DB06 的输出。

#### 禁止事项与易错点

1. DB06 必须有两条入口。
2. 配置必须与 DB04 完全一致。
3. DB05 后不能绕过 DB06 直接连接大模型。
4. 大模型必须引用 DB06，不再引用 DB04。
5. 六个查询字段必须全部选择。
6. 处理模式必须是查询数据。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] DB06 由 DB04 复制得到并正确改名。
- [ ] 如果出口直接连接 DB06。
- [ ] 否则出口经过 DB05 后连接 DB06。
- [ ] 查询范围与 DB04 完全一致。
- [ ] 六个查询字段全部选择。
- [ ] 后续预留连接 WF-09 大模型。

下一次只讨论第 8 个节点：`WF09_LLM_履历素材`。该节点确认前不提前归档具体配置。
