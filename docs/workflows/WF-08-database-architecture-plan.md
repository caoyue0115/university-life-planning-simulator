# WF-08 成长复盘：数据库架构规划记录

> 分支：`wf03-wf12-architecture-planning`
>
> 工作方式：一次只讨论并确认一个节点。当前阶段冻结可实现的表结构、输入输出、代码、分支和连线，但暂不要求在讯飞星辰平台创建数据表或完成变量绑定。
>
> 资料基线：以用户本次提供的 WF-08 系统提示词和大模型节点截图为唯一业务底稿；数据库化只迁移状态读写方式，不擅自删改原业务规则。

## 阅读与执行方式

本文供后续负责讯飞星辰平台配置的同学直接执行。每个节点确认后，必须记录：节点名称、前后连线、节点设置、数据范围或输入、字段或输出、业务作用、禁止事项与易错点、完成检查。

文档状态必须区分：

- **已确认设计**：已经与用户讨论通过，可以按文档实施。
- **草案待确认**：仅供讨论，不能直接建表或绑定变量。
- **平台已验证**：已经在讯飞星辰实际运行并通过测试。

除非明确标记“平台已验证”，不得把规划描述成已经在画布完成。

## 一、已确认的业务基线

WF-08 根据 WF-06 已确认主规划、WF-07 已确认任务和用户本轮陈述完成成长复盘，给出三种推荐之一：

- `continue`：证据支持继续当前方向；
- `adjust`：方向可保留，但行动或节奏需要调整；
- `consider_switch`：出现重要冲突，需要进一步比较，但不能直接替用户切换。

WF-08 只保存复盘结果，不直接修改 WF-06 主规划或 WF-07 任务。确认一份 adjust 或 consider_switch 复盘，也不等于执行了调整或切换。

## 二、上游信息与证据边界

| 优先级 | 数据来源 | 处理方式 |
|---:|---|---|
| 1 | WF-06 `plan_confirmed` | 对照原规划判断变化及影响 |
| 2 | WF-07 `task_confirmed` | 提取任务状态、实际证据和延期原因 |
| 3 | 用户本轮原话 | 补充数据库中没有记录的新事实 |

固定规则：

1. 没有任务记录，但用户提供具体事实时仍可复盘。
2. 任务记录缺失且用户输入空泛时进入 collecting，每轮最多询问三个事实：做了什么、结果如何、哪里卡住。
3. 没有主规划或任务都不能强制退回 WF-06/WF-07，也不能编造数据。
4. 不得修改任何上游 confirmed 数据。

## 三、事实、证据和推断必须分开

| 字段 | 允许保存的内容 |
|---|---|
| `explicit_new_facts` | 用户本轮明确陈述的事实 |
| `behavior_evidence` | 已确认任务中的行为和实际证据 |
| `agent_inferences` | 模型根据事实和证据作出的推断 |
| `changes_since_plan` | 与已确认主规划相比发生的变化 |

“我觉得”“可能”“也许”等内容不能直接作为已验证事实；没有证据的判断必须放入推断或待验证问题。

## 四、draft 与 confirmed 结构基线

```json
{
  "explicit_new_facts":[],
  "behavior_evidence":[],
  "agent_inferences":[],
  "changes_since_plan":[],
  "impact_on_plan":[],
  "推荐":"continue|adjust|consider_switch",
  "recommended_adjustments":[],
  "opportunity_costs":[],
  "questions_to_verify":[],
  "摘要":""
}
```

固定解释：

1. 原结构曾写“继续|调整|consider_switch”，而生成规则要求 `continue|adjust|consider_switch`；数据库版统一使用三个英文枚举值。
2. 字段名继续保留中文键 `推荐`，不擅自改名。
3. 原文中的“历史 confirm 数据”解释为历史 `confirmed` 数据。
4. `adjust` 只形成建议，不直接修改 WF-07。
5. `consider_switch` 只形成进一步比较建议，不直接修改 WF-06。
6. 普通数组最多 5 项，warnings 最多 3 项且去重。

## 五、已确认的状态机

模块状态统一使用：

- `not_started`
- `collecting`
- `awaiting_confirmation`
- `complete`
- `cancelled`
- 明确澄清时允许 `current_status="clarification_needed"`

确认成功时：

```text
review_draft → review_confirmed
review_draft = {}
review_status = complete
completed_workflow_add = WF-08
next_workflow = WF-12
```

modify 必须按用户纠正的事实重新分析，不得篡改用户未纠正的事实。cancel 清空 draft、保留 confirmed，不修改主规划或任务。

## 六、当前大模型节点基线

| 配置项 | 当前值 |
|---|---|
| 当前输入 | `user_input`、`router_context` |
| 当前输出格式 | text |
| 当前唯一输出 | `output`，String |
| 当前模型 | Kimi-K2.5 |
| 对话历史 | 关闭 |

数据库化后预计增加：

- `plan_state_rows`
- `task_state_rows`
- `review_state_rows`

输出仍保持单一 `output:String`。正式迁移时以用户提供的原提示词为底稿做最小修改，保留事实、证据、推断分离、三种推荐、机会成本、待验证问题、确认、修改和取消规则，只把完整 prior state 改为数据库输入与增量输出。

## 七、WF-08 模块表字段基线

建议表名：`wf08_growth_review_records`

| 字段 | 类型 | 默认值 | 用途 |
|---|---|---|---|
| `review_status` | String | `not_started` | WF-08 当前模块状态 |
| `review_draft` | String | `{}` | 收集或待确认的复盘对象 |
| `review_confirmed` | String | `{}` | 用户已确认的复盘对象 |
| `review_version` | Integer | `1` | 模块记录版本 |
| `last_user_intent` | String | `未记录` | 用户本轮原话 |
| `warnings` | String | `[]` | 本模块警告数组的 JSON 字符串 |

六个字段是后续读取、初始化、统一重新读取、大模型输入、C01 输出和模块写回的固定合同。所有复盘事实、证据、推断、影响、建议、机会成本和待验证问题都保存在 draft 或 confirmed JSON 中，不另加数据库列。

回复不写数据库，继续通过共享 `last_reply`、现有 N90 和唯一结束节点返回。

当前状态：业务基线、证据边界、状态机、结构解释和六字段模块表已经讨论确认并归档；讯飞星辰平台尚未实际建表。

## 八、预期主链路骨架

```text
M03 MAIN 总分支器的 WF-08 出口
→ 读取 WF-06 已确认主规划
→ 读取 WF-07 已确认任务
→ 读取 WF-08 复盘状态
→ 判断记录是否存在
→ 必要时初始化
→ 统一重新读取 WF-08 当前状态
→ WF-08 大模型
→ C01 解析模型输出
→ 解析是否成功
→ 更新 WF-08 模块表
→ C02 合并公共路由状态
→ 更新公共路由表
→ 写入共享 last_reply
→ 现有 N90
→ 唯一结束节点
```

本节只冻结总体方向，不代表所有节点配置已经确认。后续仍按“一次一个节点、用户确认后立即归档推送”的方式推进。

## 九、逐节点确认记录

### 第 1 个节点：`WF08_DB01_读取主规划状态`

#### 前后连线

```text
M03 MAIN 总分支器的 WF-08 出口
→ WF08_DB01_读取主规划状态
```

本节点完成后先停止；下一节点单独讨论。

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf06_main_plan_records` |
| 处理模式 | 查询数据 |
| 查询上限 | `1` |
| 排序 | 留空 |
| 输出 | 保持默认的 `isSuccess`、`message`、`outputList` |

#### 设置数据范围

| 表字段 | 条件 | 值类型 | 比较值 |
|---|---|---|---|
| `plan_version` | 大于 | 输入 | 数字 `0` |

#### 查询结果字段

- `plan_status`
- `plan_confirmed`
- `plan_version`

不读取 `plan_draft`。

#### 业务作用

WF-08 使用可解析的非空 `plan_confirmed` 对照原规划，识别用户当前行为与原目标之间的变化和影响。没有已确认主规划时仍可依据已确认任务和用户具体事实进行谨慎复盘，不能强制退回 WF-06。

#### 禁止事项与易错点

1. `plan_version > 0` 只表示记录存在，不表示主规划已确认。
2. 是否可用必须由后面的大模型检查非空 `plan_confirmed`。
3. 不得读取或修改 `plan_draft`。
4. 不得增加“无主规划返回 WF-06”的分支。
5. 本节点只查询，不写入任何数据。
6. 不得误选 WF-08 自身模块表。
7. 默认输出不得修改。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 上游连接 MAIN 总分支器的 WF-08 出口。
- [ ] 数据库选择 `university_planner`。
- [ ] 数据表选择 `wf06_main_plan_records`。
- [ ] 处理模式为查询数据。
- [ ] 查询上限为 1，排序留空。
- [ ] 数据范围为 `plan_version > 0`。
- [ ] 只读取 status、confirmed 和 version。
- [ ] 没有读取 draft。
- [ ] 默认输出保持不变。
- [ ] 没有增加主规划前置门槛。

### 第 2 个节点：`WF08_DB02_读取学期任务状态`

#### 前后连线

```text
WF08_DB01_读取主规划状态
→ WF08_DB02_读取学期任务状态
```

本节点完成后先停止；下一节点单独讨论。

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf07_semester_task_records` |
| 处理模式 | 查询数据 |
| 查询上限 | `1` |
| 排序 | 留空 |
| 输出 | 保持默认的 `isSuccess`、`message`、`outputList` |

#### 设置数据范围

| 表字段 | 条件 | 值类型 | 比较值 |
|---|---|---|---|
| `task_version` | 大于 | 输入 | 数字 `0` |

#### 查询结果字段

- `task_status`
- `task_confirmed`
- `task_version`

不读取 `task_draft`。

#### 业务作用

WF-08 从可解析的非空 `task_confirmed` 中取得已确认任务状态、用户实际完成证据、延期原因和任务内容，并把这些信息作为 `behavior_evidence` 的来源。没有已确认任务时，用户提供具体事实仍可复盘。

#### 禁止事项与易错点

1. `task_version > 0` 只表示记录存在，不表示任务已确认。
2. 是否可用必须由后面的大模型检查非空 `task_confirmed`。
3. 不得读取 `task_draft`，避免把未确认变更当作事实。
4. 不得把没有实际证据的任务自动视为完成。
5. 不得增加“必须先完成 WF-07”的分支。
6. 查不到任务也必须继续。
7. 本节点只查询，不修改任务数据。
8. 默认输出不得修改。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 上游连接 `WF08_DB01_读取主规划状态`。
- [ ] 数据表选择 `wf07_semester_task_records`。
- [ ] 处理模式为查询数据。
- [ ] 查询上限为 1，排序留空。
- [ ] 数据范围为 `task_version > 0`。
- [ ] 只读取 status、confirmed 和 version。
- [ ] 没有读取 draft。
- [ ] 默认输出保持不变。
- [ ] 没有增加任务前置门槛。

### 第 3 个节点：`WF08_DB03_读取复盘状态`

#### 前后连线

```text
WF08_DB02_读取学期任务状态
→ WF08_DB03_读取复盘状态
```

本节点完成后先停止；下一节点单独讨论。

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf08_growth_review_records` |
| 处理模式 | 查询数据 |
| 查询上限 | `1` |
| 排序 | 留空 |
| 输出 | 保持默认的 `isSuccess`、`message`、`outputList` |

#### 设置数据范围

| 表字段 | 条件 | 值类型 | 比较值 |
|---|---|---|---|
| `review_version` | 大于 | 输入 | 数字 `0` |

#### 查询结果字段

- `review_status`
- `review_draft`
- `review_confirmed`
- `review_version`
- `last_user_intent`
- `warnings`

六个业务字段必须全部读取。

#### 业务作用

本节点只探测当前用户是否已有 WF-08 模块记录，并把整个查询结果数组交给下一个“复盘状态是否存在”分支器。它不负责判断复盘是否完成，也不是大模型的最终统一输入。

#### 禁止事项与易错点

1. 六个字段必须全部读取，不能只读 `review_status`。
2. `review_version > 0` 判断记录是否存在，不判断复盘完成状态。
3. 不得在本节点增加 `review_status` 条件。
4. 不得读取系统自动字段。
5. 本节点只查询，不新增或更新数据。
6. 默认输出不得修改。
7. 后续分支器必须引用本节点的整个 `outputList`。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 上游连接 `WF08_DB02_读取学期任务状态`。
- [ ] 数据表选择 `wf08_growth_review_records`。
- [ ] 处理模式为查询数据。
- [ ] 查询上限为 1，排序留空。
- [ ] 数据范围为 `review_version > 0`。
- [ ] 六个业务字段全部读取。
- [ ] 没有读取系统自动字段。
- [ ] 默认输出保持不变。
- [ ] 后续预留连接存在性分支器。

### 第 4 个节点：`WF08_复盘状态是否存在`

#### 连线

```text
WF08_DB03_读取复盘状态
→ WF08_复盘状态是否存在
```

两个出口预留如下：

```text
如果（已有记录）
→ WF08_DB05_读取当前复盘状态
```

```text
否则（没有记录）
→ WF08_DB04_初始化复盘状态
→ WF08_DB05_读取当前复盘状态
```

#### 分支器配置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 分支器 |
| 引用变量 | `WF08_DB03_读取复盘状态.outputList` |
| 判断方式 | 数组长度 |
| 条件 | 大于 |
| 比较值类型 | Integer / 数字 |
| 比较值 | `0` |

#### 业务作用

本节点只判断 WF-08 模块记录是否存在。有记录时直接进入后面的正式统一读取；没有记录时先新增完整初始记录，再进入同一个正式读取节点。

#### 禁止事项与易错点

1. 必须判断整个 `outputList` 的长度，不得判断 `review_status`。
2. 比较值必须是数字 `0`，不能是 String `"0"`。
3. “如果”和“否则”不能接反。
4. 记录存在不等于复盘已确认或模块已完成。
5. 两条路径最终必须汇合到同一个 DB05。
6. 分支器本身不修改任何数据。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 上游连接 `WF08_DB03_读取复盘状态`。
- [ ] 引用 DB03 的整个 `outputList`。
- [ ] 判断方式为数组长度大于数字 `0`。
- [ ] “如果”出口预留给 DB05。
- [ ] “否则”出口预留给 DB04。
- [ ] 没有判断复盘状态或完成状态。
- [ ] 两个出口没有接反。

### 第 5 个节点：`WF08_DB04_初始化复盘状态`

#### 连线

```text
WF08_复盘状态是否存在.否则
→ WF08_DB04_初始化复盘状态
→ WF08_DB05_读取当前复盘状态
```

“如果”出口不经过本节点，直接进入 DB05。

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf08_growth_review_records` |
| 处理模式 | 新增数据 |
| 输出 | 保持默认的 `isSuccess`、`message`、`outputList` |

#### 新增数据字段

| 字段名 | 值类型 | 填写值 | 字段类型 |
|---|---|---|---|
| `review_status` | 输入 | `not_started` | String |
| `review_draft` | 输入 | `{}` | String |
| `review_confirmed` | 输入 | `{}` | String |
| `review_version` | 输入 | 数字 `1` | Integer |
| `last_user_intent` | 输入 | `未记录` | String |
| `warnings` | 输入 | `[]` | String |

系统自动生成的 `id`、`uid`、`create_time` 等字段不填写。

#### 业务作用

首次进入 WF-08 且没有模块记录时，为当前用户建立一条完整初始记录。初始化完成后必须进入 DB05 重新查询，不能直接连接大模型。

#### 禁止事项与易错点

1. 只能连接存在性分支器的“否则”出口。
2. 处理模式必须是新增数据。
3. `{}` 和 `[]` 是手动输入的 String 内容。
4. `review_version` 必须是 Integer `1`，不能是 String。
5. 六个业务字段必须一次填齐。
6. 不得填写系统自动字段。
7. 初始化后必须进入 DB05 重新读取。
8. 不得直接连接大模型。
9. “如果”出口不能经过本节点。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 节点连接在“否则”出口。
- [ ] 数据表选择 `wf08_growth_review_records`。
- [ ] 处理模式为新增数据。
- [ ] 六个字段全部填写。
- [ ] draft 和 confirmed 为 String `{}`。
- [ ] warnings 为 String `[]`。
- [ ] version 为 Integer `1`。
- [ ] 没有填写系统自动字段。
- [ ] 后续连接 DB05。

### 第 6 个节点：`WF08_DB05_读取当前复盘状态`

本节点可以直接复制第 3 个节点 `WF08_DB03_读取复盘状态`，再修改节点名称。

#### 两条入口连线

```text
WF08_复盘状态是否存在.如果
→ WF08_DB05_读取当前复盘状态
```

```text
WF08_复盘状态是否存在.否则
→ WF08_DB04_初始化复盘状态
→ WF08_DB05_读取当前复盘状态
```

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf08_growth_review_records` |
| 处理模式 | 查询数据 |
| 查询上限 | `1` |
| 排序 | 留空 |
| 输出 | 保持默认的 `isSuccess`、`message`、`outputList` |

#### 设置数据范围

| 表字段 | 条件 | 值类型 | 比较值 |
|---|---|---|---|
| `review_version` | 大于 | 输入 | 数字 `0` |

#### 查询结果字段

- `review_status`
- `review_draft`
- `review_confirmed`
- `review_version`
- `last_user_intent`
- `warnings`

六个业务字段必须全部读取。

#### 后续连接

```text
WF08_DB05_读取当前复盘状态
→ WF08_LLM_成长复盘
```

#### 业务作用

DB03 只探测记录是否存在；DB05 是两条路径汇合后的正式统一读取。无论用户已有记录，还是本轮刚初始化，大模型都只引用 DB05 的输出。

#### 禁止事项与易错点

1. DB05 必须有两条入口。
2. 配置必须与 DB03 完全一致。
3. DB04 后不能绕过 DB05 直接连接大模型。
4. 大模型后续必须引用 DB05，不再引用 DB03。
5. 六个查询字段必须全部选择。
6. 处理模式必须是查询数据。

#### 当前状态

- 架构与配置：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] DB05 由 DB03 复制得到。
- [ ] 节点名称修改为 `WF08_DB05_读取当前复盘状态`。
- [ ] “如果”出口直接连接 DB05。
- [ ] “否则”出口经过 DB04 后连接 DB05。
- [ ] 查询范围与 DB03 完全一致。
- [ ] 六个查询字段全部选择。
- [ ] 后续预留连接 WF-08 大模型。

### 第 7 个节点：`WF08_LLM_成长复盘`

#### 连线与节点设置

```text
WF08_DB05_读取当前复盘状态
→ WF08_LLM_成长复盘
→ WF08_C01_解析模型输出
```

保持当前 Kimi-K2.5 模型，关闭对话历史；输出格式为 text，唯一输出为 `output:String`。

#### 输入

| 参数名 | 引用来源 |
|---|---|
| `user_input` | `开始.AGENT_USER_INPUT` |
| `router_context` | `N01 路由大模型.output` |
| `plan_state_rows` | `WF08_DB01_读取主规划状态.outputList` |
| `task_state_rows` | `WF08_DB02_读取学期任务状态.outputList` |
| `review_state_rows` | `WF08_DB05_读取当前复盘状态.outputList` |

#### 完整提示词

系统提示词、用户提示词及整体复制文本单独保存在：[WF-08 数据库版大模型提示词](prompts/WF-08-database-llm-prompts.md)。执行同学必须整体复制，不得继续使用旧版完整 state 提示词。

#### 已确认边界

- 用户明确事实、已确认任务证据和模型推断必须分开。
- 推荐统一使用 `continue`、`adjust`、`consider_switch`，字段名仍保留中文 `推荐`。
- 没有任务但用户给出具体事实时仍可复盘；信息不足时 collecting 一次最多问三个事实。
- adjust 或 consider_switch 只形成复盘建议，不直接修改 WF-06 或 WF-07。
- 原始变化、影响、建议、机会成本、待验证问题、确认、修改和取消规则全部保留。
- 模型只输出十个增量字段，不输出完整 state 或 completed_workflows。
- 输出仍为单一 String `output`。

#### 当前状态

- 输入、输出和完整提示词：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 五个输入参数全部添加且使用引用。
- [ ] 自身状态引用 DB05，不引用 DB03。
- [ ] 输出仍只有 `output:String`。
- [ ] 对话历史关闭。
- [ ] 系统提示词和用户提示词整体替换。
- [ ] 不再传递或输出完整 state。
- [ ] 没有增加主规划或任务硬门槛。

### 第 8 个节点：`WF08_C01_解析模型输出`

#### 连线

```text
WF08_LLM_成长复盘
→ WF08_C01_解析模型输出
→ WF08_解析是否成功
```

#### 节点类型与输入

节点类型选择代码节点，语言使用 Python。

| 参数名 | 类型 | 引用来源 |
|---|---|---|
| `model_output` | String | `WF08_LLM_成长复盘.output` |
| `user_input` | String | `开始.AGENT_USER_INPUT` |

#### 输出

| 参数名 | 类型 | 作用 |
|---|---|---|
| `parse_ok` | Boolean | 是否通过全部解析与业务校验 |
| `error_message` | String | 失败原因；成功时为空字符串 |
| `reply` | String | 写入共享 `last_reply` 的用户回复 |
| `current_workflow` | String | 固定为 `WF-08` |
| `current_status` | String | 本轮公共路由状态 |
| `last_user_intent` | String | 用户本轮原话 |
| `review_status` | String | WF-08 模块状态 |
| `review_draft_json` | String | 压缩后的复盘草稿 JSON |
| `review_confirmed_json` | String | 压缩后的已确认复盘 JSON |
| `next_workflow` | String | `WF-08` 或 `WF-12` |
| `completed_workflow_add` | String | 空字符串或 `WF-08` |
| `warnings_json` | String | 压缩后的警告数组 JSON |

#### 独立代码文件

完整可复制代码保存在：[WF-08 C01 解析模型输出](code/WF-08-C01-parse-model-output.py)。不得从聊天记录手工拼接代码。

#### 核心校验

1. 模型输出必须恰好包含约定的十个顶层字段。
2. `current_workflow` 必须为 `WF-08`，`last_user_intent` 必须与 `user_input` 完全一致。
3. 推荐值只允许 `continue`、`adjust`、`consider_switch`。
4. collecting 阶段的推荐必须为空，不能提前下结论。
5. 完整复盘至少包含一项用户明确事实或已确认行为证据。
6. awaiting_confirmation 必须存在完整 draft；complete 必须清空 draft、保留完整 confirmed。
7. 只有 complete 才能输出 `completed_workflow_add=WF-08` 并进入 `WF-12`。
8. warnings 必须是最多三项的不重复字符串数组。
9. 支持清除模型可能附带的 JSON 代码围栏。
10. 任一校验失败时 `parse_ok=false`，返回非空安全回复，不允许进入写库成功路径。

#### 已执行测试

| 场景 | 预期 | 结果 |
|---|---|---|
| collecting 且未给推荐 | 通过 | 通过 |
| 完整 continue 复盘 | 通过 | 通过 |
| 完整 adjust 复盘 | 通过 | 通过 |
| 正式确认完成 | 通过 | 通过 |
| 缺少事实和行为证据 | 拒绝 | 拒绝 |
| 使用中文推荐值 | 拒绝 | 拒绝 |
| collecting 提前给出推荐 | 拒绝 | 拒绝 |
| complete 使用错误路由 | 拒绝 | 拒绝 |

#### 当前状态

- 节点合同、独立代码和八组测试：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 两个输入参数名称、类型和引用来源完全一致。
- [ ] 十二个输出参数全部建立，类型正确。
- [ ] Python 代码从独立文件整体复制。
- [ ] `parse_ok` 必须设置为 Boolean。
- [ ] 后续连接解析成功分支器。
- [ ] 解析失败路径不得写入模块表或公共路由表。

### 第 9 个节点：`WF08_解析是否成功`

#### 连线

```text
WF08_C01_解析模型输出
→ WF08_解析是否成功
```

#### 分支条件

| 设置项 | 配置 |
|---|---|
| 判断变量 | 引用 `WF08_C01_解析模型输出.parse_ok` |
| 判断条件 | 等于 |
| 比较值类型 | Boolean |
| 比较值 | `true` |

#### 两个出口

```text
如果（parse_ok = true）
→ WF08_DB06_更新复盘状态
```

```text
否则
→ WF08_写入_last_reply
```

否则出口直接使用 C01 生成的安全 `reply`，不新增大模型或消息节点。

#### 禁止事项与易错点

1. 比较值必须是 Boolean `true`，不能是字符串 `"true"`。
2. 失败路径不得连接模块表更新节点。
3. 失败路径不得连接公共路由表更新节点。
4. 本分支器只负责放行或拦截，不修改任何数据。
5. 初始化状态是否存在的分支器不能接到这里。

#### 当前状态

- 分支条件和两条出口：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 引用的是 C01 的 `parse_ok`。
- [ ] 比较值类型为 Boolean。
- [ ] 如果出口预留连接 DB06。
- [ ] 否则出口预留连接共享回复写入节点。
- [ ] 失败路径没有任何数据库更新节点。

### 第 10 个节点：`WF08_DB06_更新复盘状态`

#### 连线

```text
WF08_解析是否成功.如果
→ WF08_DB06_更新复盘状态
→ WF08_C02_合并路由状态
```

#### 节点基础设置

| 设置项 | 配置 |
|---|---|
| 节点类型 | 数据库 |
| 模式 | 表单处理数据 |
| 数据库 | `university_planner` |
| 数据表 | `wf08_growth_review_records` |
| 处理模式 | 更新数据 |

#### 设置数据范围

| 表字段 | 条件 | 值类型 | 比较值 |
|---|---|---|---|
| `review_version` | 大于 | 输入 | 数字 `0` |

#### 更新字段

| 表字段 | 值类型 | 引用来源 |
|---|---|---|
| `review_status` | 引用 | `WF08_C01_解析模型输出.review_status` |
| `review_draft` | 引用 | `WF08_C01_解析模型输出.review_draft_json` |
| `review_confirmed` | 引用 | `WF08_C01_解析模型输出.review_confirmed_json` |
| `last_user_intent` | 引用 | `WF08_C01_解析模型输出.last_user_intent` |
| `warnings` | 引用 | `WF08_C01_解析模型输出.warnings_json` |

`review_version` 本轮保持不变，不列入更新字段。

#### 禁止事项与易错点

1. 本节点只能连接解析成功分支的如果出口。
2. 不得把大模型原始 `output` 直接写入数据库。
3. draft、confirmed 和 warnings 必须引用 C01 压缩后的字符串输出。
4. 不更新公共路由表、WF-06 主规划、WF-07 学期任务或共享回复变量。
5. 不把 `review_version > 0` 误解为模块已经完成。

#### 当前状态

- 数据范围和五个更新字段：已讨论确认。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 处理模式选择更新数据。
- [ ] 数据范围为 `review_version > 0`。
- [ ] 五个字段全部引用 C01 对应输出。
- [ ] 没有更新 `review_version`。
- [ ] 前方只连接解析成功的如果出口。
- [ ] 后续预留连接 C02。

### 第 11 个节点：`WF08_C02_合并路由状态`

#### 连线

沿用已经确认的公共结构，不增加合并成功分支器：

```text
WF08_DB06_更新复盘状态
→ WF08_C02_合并路由状态
→ WF08_DB07_更新路由状态
```

C02 异常时直接中断，阻止 DB07 写入错误数据。

#### 输入

| 参数名 | 类型 | 引用来源 |
|---|---|---|
| `route_state_rows` | Array/Object | MAIN 当前路由状态查询节点 DB03 的 `outputList` |
| `completed_workflow_add` | String | `WF08_C01_解析模型输出.completed_workflow_add` |

`route_state_rows` 不能引用 WF-08 的 DB05，也不能引用路由大模型的文本输出。

#### 输出

| 输出变量 | 类型 |
|---|---|
| `merge_ok` | Boolean |
| `error_message` | String |
| `completed_workflows_json` | String |
| `state_version_next` | Integer |

#### 完整代码

完整可复制代码保存在：[WF-08 C02 合并路由代码](code/WF-08-C02-merge-route-state.py)。

代码兼容数组、对象或 JSON 字符串格式的路由记录；保留并去重历史完成列表；只允许按需追加 WF-08；将公共 `state_version` 加一；异常时直接抛错中断。

#### 单节点测试

| 测试 | 结果 |
|---|---|
| 在历史完成列表后追加 WF-08 | 通过 |
| 历史列表已有重复 WF-08 | 通过，正确去重 |
| `completed_workflow_add` 为空 | 通过，只保留历史并递增版本 |
| 路由记录为空 | 正确中断 |
| 错误传入其他工作流完成标记 | 正确中断 |
| `state_version` | 正确加一 |

#### 禁止事项与易错点

1. 下一数据库节点不得把完成列表写死为 `["WF-08"]`。
2. 不得引用 WF-08 模块表作为公共路由状态。
3. 完成标记必须引用 C01，不能固定输入 WF-08。
4. 不得删除旧完成列表。
5. 不增加 C02 成功保护分支，采用失败即中断。
6. 平台搭建时仍须先使用节点自身输入测试运行一次。
7. 独立 Python 文件必须与架构说明一起推送。

#### 当前状态

- 代码、输入、输出和接口：已讨论确认。
- 单节点测试：正常、重复和异常样例均符合预期。
- GitHub 归档：已记录。
- 星辰平台实际搭建：当前不作要求。

#### 完成检查

- [ ] 两个输入名称和类型正确。
- [ ] 路由记录引用 MAIN 的 DB03。
- [ ] 完成标记引用 C01。
- [ ] 四个输出名称和类型正确。
- [ ] 异常时直接中断。
- [ ] 后续预留连接 DB07。
- [ ] 没有增加新的保护分支器。
- [ ] 独立代码文件已归档。

## 十、下一步

下一次只讨论第 12 个节点：`WF08_DB07_更新路由状态`。该节点确认前不提前归档具体配置。
